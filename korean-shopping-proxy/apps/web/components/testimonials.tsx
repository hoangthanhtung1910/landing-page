import Image from "next/image"
import { Star } from "lucide-react"
import type { CustomerReview } from "@vyvy/content-types"
export function Testimonials({ items }: { items: CustomerReview[] }) { return <section id="danh-gia" className="scroll-mt-20 bg-card"><div className="mx-auto w-full max-w-6xl px-4 py-16 sm:px-6 lg:py-24"><div className="mx-auto max-w-2xl text-center"><p className="text-sm font-semibold uppercase tracking-wider text-primary">Khách hàng nói gì</p><h2 className="mt-3 text-3xl font-extrabold tracking-tight sm:text-4xl">Đánh giá đã được khách hàng đồng ý công khai</h2></div>{items.length===0?<div className="mx-auto mt-10 max-w-2xl rounded-2xl border border-dashed border-border p-8 text-center text-muted-foreground">Chưa có đánh giá đã xác minh để công khai. Chúng tôi không sử dụng lời chứng thực giả.</div>:<div className="mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{items.map((item)=><figure key={item.id} className="flex flex-col rounded-2xl border border-border bg-background p-6">{item.rating?<div className="flex gap-1" aria-label={`${item.rating} trên 5 sao`}>{Array.from({length:5}).map((_,index)=><Star key={index} className={`size-4 ${index<item.rating! ? "fill-primary text-primary":"text-muted"}`}/>)}</div>:null}<blockquote className="mt-4 flex-1 text-sm leading-relaxed">{item.text}</blockquote><figcaption className="mt-6 flex items-center gap-3">{item.avatar?<Image src={item.avatar.src} alt={item.avatar.alt} width={44} height={44} className="size-11 rounded-full object-cover"/>:<span className="flex size-11 items-center justify-center rounded-full bg-accent font-bold">{item.name.slice(0,1)}</span>}<span><strong className="block text-sm">{item.name}</strong>{item.location?<span className="text-xs text-muted-foreground">{item.location}</span>:null}</span></figcaption></figure>)}</div>}</div></section> }
