"use client"
import { useState } from "react"
import { ChevronDown } from "lucide-react"
import type { FaqItem } from "@vyvy/content-types"
export function Faq({ items }: { items: FaqItem[] }) { const[open,setOpen]=useState<number|null>(items.length?0:null); return <section id="hoi-dap" className="scroll-mt-20"><div className="mx-auto w-full max-w-3xl px-4 py-16 sm:px-6 lg:py-24"><div className="text-center"><p className="text-sm font-semibold uppercase tracking-wider text-primary">Hỏi đáp</p><h2 className="mt-3 text-3xl font-extrabold tracking-tight sm:text-4xl">Câu hỏi thường gặp</h2></div>{items.length===0?<p className="mt-10 text-center text-muted-foreground">Nội dung hỏi đáp đang được cập nhật.</p>:<div className="mt-10 flex flex-col gap-3">{items.map((item,index)=>{const expanded=open===index;return <article key={item.id} className="overflow-hidden rounded-2xl border border-border bg-card"><h3><button type="button" onClick={()=>setOpen(expanded?null:index)} aria-expanded={expanded} aria-controls={`faq-answer-${item.id}`} className="flex min-h-12 w-full items-center justify-between gap-4 px-5 py-4 text-left font-semibold"><span>{item.question}</span><ChevronDown className={`size-5 shrink-0 transition-transform ${expanded?"rotate-180":""}`} aria-hidden="true"/></button></h3>{expanded?<div id={`faq-answer-${item.id}`} className="px-5 pb-5 text-sm leading-relaxed text-muted-foreground">{item.answer}</div>:null}</article>})}</div>}</div></section> }
