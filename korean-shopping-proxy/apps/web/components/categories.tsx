import Image from "next/image"
import type { ProductCategory } from "@vyvy/content-types"
export function Categories({ items }: { items: ProductCategory[] }) { return <section id="danh-muc" className="scroll-mt-20"><div className="mx-auto w-full max-w-6xl px-4 py-16 sm:px-6 lg:py-24"><div className="mx-auto max-w-2xl text-center"><p className="text-sm font-semibold uppercase tracking-wider text-primary">Danh mục nổi bật</p><h2 className="mt-3 text-3xl font-extrabold tracking-tight sm:text-4xl">Khám phá sản phẩm từ Hàn Quốc</h2></div>{items.length===0?<p className="mt-10 text-center text-muted-foreground">Danh mục sản phẩm đang được cập nhật.</p>:<div className="mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{items.map((item)=><article key={item.id} className="overflow-hidden rounded-2xl border border-border bg-card"><div className="relative aspect-[4/3]"><Image src={item.image.src} alt={item.image.alt} fill sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 25vw" className="object-cover"/></div><div className="p-5"><h3 className="font-bold">{item.name}</h3>{item.blurb?<p className="mt-2 text-sm text-muted-foreground">{item.blurb}</p>:null}</div></article>)}</div>}</div></section> }
